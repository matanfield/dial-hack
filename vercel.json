{
  "version": 2,
  "buildCommand": "pnpm typecheck",
  "installCommand": "pnpm install --frozen-lockfile",
  "functions": {
    "api/index.ts": { "includeFiles": "src/dashboard/static/**" }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
